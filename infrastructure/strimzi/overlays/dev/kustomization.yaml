apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kafka-dev # Deploys this stack to the 'kafka-dev' namespace
resources:
- ../../base
patchesStrategicMerge:
- patch-operator-namespace.yaml
- patch-kafka-cluster.yaml
- patch-kafka-nodepool.yaml